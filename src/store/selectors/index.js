export const selectTableItems = (state) => state.app.items;
export const selectCurrentPage = (state) => state.app.currentPage;
export const selectPageIndex = (state) => state.app.pageIndex;
export const selectPageCount = (state) => state.app.pageCount;
